---
phase: 18.1-internationalization
plan: 04
type: execute
---

<objective>
Complete translations for all secondary pages (Voice, Documents, Settings, Contacts).

Purpose: Finish i18n coverage for the entire application.
Output: All pages fully translated, complete i18n implementation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18.1-internationalization/18.1-RESEARCH.md
@.planning/phases/18.1-internationalization/18.1-03-SUMMARY.md

**Pages to translate:**
Voice module:
- voice-agents/page.tsx
- call-logs/page.tsx
- phone-numbers/page.tsx
- voice-settings/page.tsx

Documents module:
- upload/page.tsx (documents)
- templates/page.tsx
- processing/page.tsx
- data/page.tsx

Other:
- contacts/page.tsx
- settings/page.tsx
- agents/[id]/edit/page.tsx (agent edit)

**Pattern established in Plan 03:**
- Import useTranslations from 'next-intl'
- Import setRequestLocale from 'next-intl/server'
- Call setRequestLocale(locale) at page top
- Use t('key') for all strings
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate Voice module pages</name>
  <files>src/app/[locale]/(main)/voice-agents/page.tsx, src/app/[locale]/(main)/call-logs/page.tsx, src/app/[locale]/(main)/phone-numbers/page.tsx, src/app/[locale]/(main)/voice-settings/page.tsx, src/messages/*.json</files>
  <action>
1. Add Voice namespace to translation files:
   **VoiceAgents:**
   - title, createAgent, editAgent, testAgent
   - Status: online, offline, busy, error
   - Fields: name, voice, personality, greeting
   - Actions: deploy, pause, delete

   **CallLogs:**
   - title, noCallsFound, filterByDate, filterByAgent
   - Call details: duration, direction, status, recording
   - Status: completed, missed, failed, inProgress
   - Direction: inbound, outbound
   - Export: exportCalls, downloadRecording

   **PhoneNumbers:**
   - title, addNumber, assignAgent, releaseNumber
   - Fields: number, country, type, assignedAgent
   - Type: local, tollFree, mobile

   **VoiceSettings:**
   - title, generalSettings, voicePreferences
   - Settings: defaultVoice, callRecording, transcription, webhookUrl
   - Save/cancel buttons

2. Move all Voice pages to [locale] segment and add translations:
   - Import useTranslations, setRequestLocale
   - Call setRequestLocale(locale)
   - Replace all hardcoded strings with t() calls

3. Update any Voice-specific components with translations.

**Avoid:** Do NOT miss any strings - audit each page thoroughly for hardcoded text.
  </action>
  <verify>All Voice pages render in EN/TR/AR with no hardcoded text</verify>
  <done>Voice module pages fully translated (voice-agents, call-logs, phone-numbers, voice-settings)</done>
</task>

<task type="auto">
  <name>Task 2: Translate Documents module and remaining pages</name>
  <files>src/app/[locale]/(main)/upload/page.tsx, src/app/[locale]/(main)/templates/page.tsx, src/app/[locale]/(main)/processing/page.tsx, src/app/[locale]/(main)/data/page.tsx, src/app/[locale]/(main)/contacts/page.tsx, src/app/[locale]/(main)/settings/page.tsx, src/messages/*.json</files>
  <action>
1. Add Documents namespace to translation files:
   **Upload (Documents):**
   - title, dragDropHere, uploadFiles, supportedFormats
   - Status: uploading, processing, completed, failed
   - Actions: upload, cancel, retry

   **Templates:**
   - title, createTemplate, editTemplate, deleteTemplate
   - Fields: name, description, fields, outputFormat
   - fieldTypes: text, number, date, currency, table

   **Processing:**
   - title, processingQueue, noDocumentsInQueue
   - Status: queued, processing, completed, failed
   - Actions: retryAll, clearCompleted

   **Data:**
   - title, extractedData, noData, exportData
   - Actions: view, edit, export, delete
   - Export formats: csv, json, excel

2. Add Contacts namespace:
   - title, addContact, editContact, deleteContact
   - Fields: name, email, phone, company, notes
   - Status: active, blocked
   - Actions: message, call, view

3. Add Settings namespace:
   - title, profile, preferences, integrations, security
   - Profile: name, email, avatar, timezone, language
   - Preferences: notifications, theme, defaultView
   - Security: changePassword, twoFactor, sessions

4. Move all pages to [locale] segment and add translations.

5. Update agent edit page (agents/[id]/edit):
   - Add AgentEdit namespace with form labels and buttons

**Critical:** Settings page should show Language preference using the LanguageSwitcher or similar UI.

**Avoid:** Do NOT miss form validation messages - add them to Common namespace.
  </action>
  <verify>All remaining pages render in EN/TR/AR with no hardcoded text</verify>
  <done>Documents module, Contacts, Settings, and Agent edit pages fully translated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete i18n implementation for entire application</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Test all pages in Arabic (/ar):
       - /ar/dashboard - Arabic RTL
       - /ar/inbox - Arabic RTL
       - /ar/agents - Arabic RTL
       - /ar/contacts - Arabic RTL
       - /ar/voice-agents - Arabic RTL
       - /ar/call-logs - Arabic RTL
       - /ar/phone-numbers - Arabic RTL
       - /ar/upload - Arabic RTL
       - /ar/templates - Arabic RTL
       - /ar/settings - Arabic RTL
    3. Test all pages in Turkish (/tr):
       - Verify all text is Turkish
       - Verify LTR layout
    4. Test all pages in English (/en or /):
       - Verify all text is English
       - Verify LTR layout
    5. Test LanguageSwitcher:
       - Switch between languages
       - Verify URL updates correctly
       - Verify page content changes instantly
    6. Final checks:
       - No console errors
       - No missing translations (no translation keys visible)
       - Arabic font renders correctly
       - All forms and buttons work
  </how-to-verify>
  <resume-signal>Type "approved" if all translations and RTL work correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] All Voice module pages translated
- [ ] All Documents module pages translated
- [ ] Contacts page translated
- [ ] Settings page translated
- [ ] Agent edit page translated
- [ ] No hardcoded text anywhere
- [ ] Human verification passed for all languages
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Entire application translated to EN/TR/AR
- RTL works correctly in Arabic
- LanguageSwitcher works across all pages
- Phase 18.1 complete
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-internationalization/18.1-04-SUMMARY.md`:

# Phase 18.1 Plan 04: Secondary Pages Translation Summary

**[Substantive one-liner - e.g., "Complete i18n implementation with 250+ strings across EN/TR/AR"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]
- [Key outcome 3]

## Files Created/Modified

- [List of all modified pages]
- [Translation file updates]

## Translation Statistics

- Total strings: [count]
- Namespaces: [list]
- Languages: English, Turkish, Arabic

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Phase 18.1 Complete

Full i18n support implemented:
- 3 languages: English (default), Turkish, Arabic
- RTL support for Arabic
- LanguageSwitcher in header
- All pages translated

## Next Phase

Phase 18.1 complete, ready for Phase 19 (Performance & Polish)
</output>

---
phase: 08-platform-restructure
plan: 03
type: auto
---

<objective>
Extend design system with service-specific colors (teal for Voice, orange for Documents) and update all relevant components to use new color scheme.

Purpose: Establish visual identity for multi-service platform so customers can quickly identify which service they're in.

Output: Updated color definitions in globals.css, extended Tailwind config, and styled component examples for each service.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css

**Current design system:**
- Primary gradient: blue (#3B82F6) to purple (#8B5CF6)
- Accent color: defined in Tailwind config
- Hover states: white/10 overlay
- Dark backgrounds: #1a1a2e → #16213e gradient
- Text: white/60 for inactive, white for active

**Service colors to add:**
- Conversational (existing): Accent gradient (blue/purple)
- Voice: Teal gradient (#10B981 → #06B6D4)
- Documents: Orange gradient (#F59E0B → #EF4444)

**Design patterns to extend:**
- Gradient borders on service cards
- Shadow effects tinted to service color
- Icons with service color tint on hover
- Badge backgrounds using service colors
- Underline/highlight effects with service colors

**Constraints:**
- Don't break existing components (backward compatible)
- Use Tailwind's extend feature, not overwrites
- Add CSS custom properties for gradients
- Maintain animation/transition patterns from v1.0
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add service color definitions to globals.css and Tailwind config</name>
  <files>src/app/globals.css, tailwind.config.ts</files>
  <action>
  Update src/app/globals.css to add service colors:

  Add CSS custom properties for gradients:
  ```css
  :root {
    --service-conversational-from: #3B82F6;  /* blue */
    --service-conversational-to: #8B5CF6;    /* purple */
    --service-voice-from: #10B981;           /* teal */
    --service-voice-to: #06B6D4;             /* cyan */
    --service-documents-from: #F59E0B;       /* amber */
    --service-documents-to: #EF4444;         /* red */
  }
  ```

  Update tailwind.config.ts to extend colors:
  ```js
  colors: {
    'service-voice': {
      50: '#F0FDF4',
      500: '#10B981',
      600: '#059669',
    },
    'service-documents': {
      50: '#FEF3C7',
      500: '#F59E0B',
      600: '#D97706',
    },
  }
  ```

  Add gradient utilities to extend Tailwind:
  ```js
  backgroundImage: {
    'gradient-voice': 'linear-gradient(to right, #10B981, #06B6D4)',
    'gradient-documents': 'linear-gradient(to right, #F59E0B, #EF4444)',
  }
  ```

  After update: tailwind types should include new utilities like `bg-gradient-voice`, `from-service-voice-500`, etc.
  </action>
  <verify>
  Build and check:
  - npm run build succeeds
  - No CSS errors
  - New color utilities are available in IntelliSense
  </verify>
  <done>CSS custom properties and Tailwind config updated with service colors</done>
</task>

<task type="auto">
  <name>Task 2: Create service-specific styling examples and update component usage</name>
  <files>src/components/layout/Sidebar.tsx, src/app/(main)/dashboard/page.tsx</files>
  <action>
  Update existing components to use new service colors:

  In Sidebar.tsx:
  - Update module.color references to use new Tailwind utilities
  - Voice module icon: tint to teal on hover (shadow-lg shadow-service-voice-500/50)
  - Documents module icon: tint to orange on hover (shadow-lg shadow-service-documents-500/50)
  - Conversational: keep existing accent shadow

  In Dashboard:
  - Service cards: Add gradient borders using new utilities
    - Conversational card: border-gradient (use existing accent)
    - Voice card: border-gradient-voice
    - Documents card: border-gradient-documents
  - Update shadow effects to match service colors
  - Activity feed icons: Use service-specific colors
    - 💬 Conversational: accent color
    - 📞 Voice: teal color
    - 📄 Documents: orange color

  Implementation approach:
  - Use Tailwind utility classes where possible
  - For custom gradients, use `bg-gradient-{service}` utilities
  - For shadows, use `shadow-service-{color}-500/50` opacity effect
  - Keep animation timing consistent with v1.0 (200-300ms)

  Do NOT use inline styles. All styling through Tailwind utilities.
  </action>
  <verify>
  npm run dev && navigate to pages:
  - Sidebar: module icons show correct colors on hover
  - Dashboard: service cards show correct gradient colors
  - Activity feed icons are color-coded
  - All colors are vibrant and consistent
  - No console errors or missing utilities
  </verify>
  <done>Components updated to use service-specific colors with correct styling and no console errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors in color configuration
- [ ] Service colors display correctly in all components
- [ ] Sidebar module icons use correct colors on hover
- [ ] Dashboard cards show service-specific gradients
- [ ] Activity feed icons are color-coded
- [ ] Responsive design maintained
- [ ] No visual regressions in existing components
</verification>

<success_criteria>

- Service color definitions added to CSS and Tailwind config
- New Tailwind utilities available for service colors
- Sidebar and Dashboard components updated to use new colors
- Visual consistency achieved across service modules
- No breaking changes to existing components
- Ready for Phase 9 (Voice Agents UI)

</success_criteria>

<output>
After completion, create `.planning/phases/08-platform-restructure/08-03-SUMMARY.md`:

# Phase 8 Plan 3: Design System Extension Summary

**Extended design system with service-specific colors and applied them across all relevant components.**

## Accomplishments

- Added CSS custom properties for service colors (conversational, voice, documents)
- Extended Tailwind config with service color utilities and gradient backgrounds
- Updated Sidebar to use teal icons for Voice, orange for Documents
- Updated Dashboard service cards to display gradient borders matching service colors
- Applied color coding to activity feed icons

## Files Created/Modified

- `src/app/globals.css` - Added CSS custom properties for service colors
- `tailwind.config.ts` - Extended Tailwind config with service colors and gradients
- `src/components/layout/Sidebar.tsx` - Applied service colors to module icons
- `src/app/(main)/dashboard/page.tsx` - Applied service colors to cards and activity feed

## Decisions Made

- Used CSS custom properties for reusable gradients
- Extended Tailwind config instead of overwriting defaults (backward compatible)
- Applied colors to shadows and borders for visual consistency
- Kept animation timing consistent with v1.0 patterns

## Issues Encountered

None

## Next Phase Readiness

Phase 8 complete. Platform restructure foundation ready for services UI implementation.
Next: Phase 9 (Voice Agents UI - agents page, call logs, phone numbers)
</output>

---
phase: 04-grid-scale
plan: 02
type: execute
---

<objective>
Apply responsive padding and layout fixes to Voice/Documents section pages.

Purpose: Phone Numbers, Documents, Processing, and Data pages all have fixed p-8 padding and layouts that don't adapt well to mobile. Apply consistent responsive patterns.
Output: All Document Intelligence and remaining Voice pages fully responsive on 320px+ screens.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-grid-scale/18-01-SUMMARY.md

# Pages to update:
@src/app/(main)/phone-numbers/page.tsx
@src/app/(main)/documents/page.tsx
@src/app/(main)/processing/page.tsx
@src/app/(main)/data/page.tsx

# Established patterns:
- Responsive padding: p-4 sm:p-6 lg:p-8
- Stats grid: grid-cols-2 md:grid-cols-4 (not 1 → 4)
- Touch targets: min-h-[44px] for buttons
- Responsive text: text-xl sm:text-2xl lg:text-3xl for stat numbers
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phone Numbers page responsive fixes</name>
  <files>src/app/(main)/phone-numbers/page.tsx</files>
  <action>
Apply responsive patterns:

1. Container padding:
   - Change `p-8` (line ~131) to `p-4 sm:p-6 lg:p-8`

2. Stats grid:
   - Change `grid-cols-1 md:grid-cols-4` to `grid-cols-2 md:grid-cols-4`
   - Stats should show 2 columns on mobile (not 1)

3. Stat card responsiveness:
   - Padding: Change `p-6` to `p-3 sm:p-4 lg:p-6`
   - Icon container: Change `w-12 h-12` to `w-8 h-8 sm:w-10 sm:h-10 lg:w-12 lg:h-12`
   - Stat number: Change `text-3xl` to `text-xl sm:text-2xl lg:text-3xl`
   - Label: Change `text-sm` to `text-[10px] sm:text-xs lg:text-sm`

4. Header section:
   - Make responsive with flex-col sm:flex-row pattern
   - Button full-width on mobile: `w-full sm:w-auto`

5. Phone number cards grid:
   - Already uses `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` (good)
   - Card padding: Change `p-6` to `p-4 sm:p-5 lg:p-6`

6. Action buttons touch targets:
   - Ensure all action buttons have `min-h-[44px]`

Do NOT modify the pause/delete logic or mock data.
  </action>
  <verify>npm run build succeeds</verify>
  <done>
- Container has responsive padding
- Stats show in 2-column grid on mobile
- No horizontal scroll at 320px
  </done>
</task>

<task type="auto">
  <name>Task 2: Documents and Processing pages responsive fixes</name>
  <files>src/app/(main)/documents/page.tsx, src/app/(main)/processing/page.tsx</files>
  <action>
Apply same responsive patterns to both pages:

**Documents page (src/app/(main)/documents/page.tsx):**

1. Container padding:
   - Change `p-8` to `p-4 sm:p-6 lg:p-8`

2. Stats grid:
   - Change `grid-cols-1 md:grid-cols-4` to `grid-cols-2 md:grid-cols-4`

3. Stat cards (same as Phone Numbers):
   - Padding: `p-3 sm:p-4 lg:p-6`
   - Icon: `w-8 h-8 sm:w-10 sm:h-10 lg:w-12 lg:h-12`
   - Number: `text-xl sm:text-2xl lg:text-3xl`
   - Label: `text-[10px] sm:text-xs lg:text-sm`

4. Upload zone and template selector grid:
   - Change `grid-cols-1 lg:grid-cols-3` to include tablet breakpoint
   - Upload zone: `lg:col-span-2` is fine
   - Reduce upload zone padding on mobile: `p-4 sm:p-6 lg:p-8`

5. Upload zone center content:
   - Icon size: `w-12 h-12 sm:w-16 sm:h-16 lg:w-20 lg:h-20`
   - Title: `text-lg sm:text-xl`
   - Button: `min-h-[44px]`

6. Recent uploads grid:
   - Change `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` (good)
   - Card padding: `p-4 sm:p-5`

**Processing page (src/app/(main)/processing/page.tsx):**

1. Container padding:
   - Change `p-8` to `p-4 sm:p-6 lg:p-8`

2. Stats grid:
   - Change `grid-cols-1 md:grid-cols-4` to `grid-cols-2 md:grid-cols-4`

3. Stat cards (same pattern)

4. Filter section:
   - Make filter buttons wrap on mobile:
   - Change `flex gap-3` to `flex flex-wrap gap-2 sm:gap-3`
   - Filter buttons: add `min-h-[36px]` for touch targets

5. Job cards:
   - Padding: Change `p-8` to `p-4 sm:p-6 lg:p-8`
   - Stage pills: make smaller on mobile with `text-[10px] sm:text-xs`
   - Progress grid: already uses `grid-cols-1 lg:grid-cols-2` (good)

Do NOT modify upload/processing logic or mock data.
  </action>
  <verify>npm run build succeeds for both files</verify>
  <done>
- Both pages have responsive padding cascade
- Stats in 2-column grid on mobile
- Filter buttons wrap gracefully on Processing page
- Upload zone readable on mobile
  </done>
</task>

<task type="auto">
  <name>Task 3: Extracted Data page responsive fixes</name>
  <files>src/app/(main)/data/page.tsx</files>
  <action>
Apply responsive patterns:

1. Container padding:
   - Change `p-8` to `p-4 sm:p-6 lg:p-8`

2. Stats grid:
   - Change `grid-cols-1 md:grid-cols-4` to `grid-cols-2 md:grid-cols-4`

3. Stat cards (same pattern as other pages)

4. Search/filter section:
   - Grid: Change `grid-cols-1 md:grid-cols-3` to stack better
   - Search should be full width on mobile
   - Filter dropdown: full width on mobile `w-full`

5. Data list items:
   - Padding: Change `p-6` to `p-4 sm:p-5 lg:p-6`
   - Confidence display: hide on mobile, show on tablet+
   - Action buttons: ensure `min-h-[44px]`

6. Detail panel (critical fix):
   - Current: `fixed right-0 top-0 h-full w-full max-w-2xl`
   - Make full-screen on mobile:
   ```tsx
   className="fixed inset-0 z-50 bg-white dark:bg-slate-800 tablet:right-0 tablet:left-auto tablet:w-full tablet:max-w-2xl tablet:rounded-l-3xl"
   ```
   - Add close button visibility on mobile
   - Padding: Change `p-8` to `p-4 sm:p-6 lg:p-8`

7. Fields preview grid:
   - Change `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` to `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`

Do NOT modify search/filter logic or mock data.
  </action>
  <verify>npm run build succeeds</verify>
  <done>
- Stats in 2-column grid on mobile
- Detail panel is full-screen on mobile
- Search/filter stack properly on mobile
- No horizontal scroll at 320px
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive layouts for Phone Numbers, Documents, Processing, and Data pages</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Test each page at 375px viewport:
       - /phone-numbers - Stats 2-col, cards stack, no overflow
       - /documents - Upload zone usable, stats 2-col
       - /processing - Filter buttons wrap, job cards readable
       - /data - Click item → full-screen detail on mobile
    3. Test at 768px (tablet):
       - Stats 4-col
       - Detail panels have max-width
    4. Test at 1024px+ (desktop):
       - Layout unchanged from before
       - Proper padding and spacing
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors in any modified files
- [ ] All 4 pages have no horizontal scroll at 320px
- [ ] Stats grids show 2 columns on mobile
- [ ] Touch targets meet WCAG 2.5.8
- [ ] Desktop layouts unchanged
</verification>

<success_criteria>
- All Document Intelligence pages (documents, processing, data) responsive
- Phone Numbers page responsive
- Consistent padding cascade across all pages
- Detail panels work on mobile (full-screen pattern)
- Phase 18 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-grid-scale/18-02-SUMMARY.md`
</output>

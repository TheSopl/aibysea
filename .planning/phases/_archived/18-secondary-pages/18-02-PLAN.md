---
phase: 18-secondary-pages
plan: 02
type: execute
---

<objective>
Make Voice module pages (Voice Agents, Phone Numbers, Voice Settings) fully responsive with mobile-first design patterns.

Purpose: Ensure Voice module provides consistent mobile experience matching established patterns from Phase 17-18.
Output: Three responsive Voice module pages with proper touch targets and mobile layouts.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior work establishing patterns:**
@.planning/phases/04-grid-scale/18-01-SUMMARY.md
@.planning/phases/03-view-adapt/17-01-SUMMARY.md

**Source files to modify:**
@src/app/(main)/voice-agents/page.tsx
@src/app/(main)/phone-numbers/page.tsx
@src/app/(main)/voice-settings/page.tsx

**Established patterns from prior phases:**
- Master-detail toggle: list hidden with `selectedItem && "hidden tablet:flex"`
- Mobile overlay: `fixed inset-0 z-40 + tablet:relative tablet:w-96`
- Touch target minimums: min-h-[44px] for buttons, min-h-[72px] for list items
- Responsive padding: p-4 sm:p-6 lg:p-8
- Stats grid: grid-cols-2 md:grid-cols-4 with responsive padding/sizing
- Back button pattern: lg:hidden with ChevronLeft icon
</context>

<tasks>

<task type="auto">
  <name>Task 1: Voice Agents responsive layout with master-detail pattern</name>
  <files>src/app/(main)/voice-agents/page.tsx</files>
  <action>
Apply established responsive patterns to Voice Agents page:

1. **Master-detail pattern for mobile:**
   - Add `cn` utility import from lib/utils
   - Add state for tracking if detail panel should show on mobile: use existing selectedAgent
   - Wrap agents list with conditional class: `className={cn("flex-1 min-w-0", selectedAgent && "hidden lg:block")}`
   - Detail panel: Add mobile-first layout with `fixed inset-0 z-40 bg-white dark:bg-slate-800 lg:relative lg:w-96 lg:z-auto`
   - Add back button at top of detail panel: ChevronLeft icon, onClick to deselect, `lg:hidden` class

2. **Stats cards (already has grid-cols-2 md:grid-cols-4):**
   - Stats already have responsive sizing, verify touch targets

3. **Agent list items:**
   - Ensure min-h-[72px] for list items (currently p-3 sm:p-4 lg:p-5, may need adjustment)
   - Action buttons: ensure min-h-[44px] min-w-[44px] touch targets
   - Current button padding is p-1.5 sm:p-2 - change to p-2.5 with min-h-[44px] min-w-[44px]

4. **Create button:**
   - Already has responsive text, ensure min-h-[44px]

Note: Voice Agents already has good responsive foundations (p-4 sm:p-6 lg:p-8, responsive stats). Focus on master-detail pattern and touch targets.
  </action>
  <verify>
- Open at 375px width: only agent list visible
- Click agent: list hides, detail panel fills screen with back button
- Click back: returns to list
- Touch targets visually large enough (44px minimum)
- Open at 1024px+: side-by-side layout restored
  </verify>
  <done>
- Mobile shows list OR detail (not both)
- Back button visible only on mobile
- All action buttons meet 44px touch target
- List items meet 72px touch height
  </done>
</task>

<task type="auto">
  <name>Task 2: Phone Numbers and Voice Settings responsive</name>
  <files>src/app/(main)/phone-numbers/page.tsx, src/app/(main)/voice-settings/page.tsx</files>
  <action>
**Phone Numbers page:**

1. **Responsive padding:**
   - Change `p-8` to `p-4 sm:p-6 lg:p-8`

2. **Stats grid (already grid-cols-1 md:grid-cols-4):**
   - Change to `grid-cols-2 md:grid-cols-4` for better mobile layout
   - Responsive stat card padding: `p-4 sm:p-6`
   - Responsive icon size: `w-10 h-10 sm:w-12 sm:h-12`
   - Responsive text: `text-2xl sm:text-3xl` for numbers

3. **Header section:**
   - Stack on mobile: `flex flex-col sm:flex-row sm:items-center justify-between gap-3 sm:gap-0`
   - Full-width button on mobile: add `w-full sm:w-auto` to Add button, `justify-center` for centered text

4. **Cards grid (already grid-cols-1 md:grid-cols-2 lg:grid-cols-3):**
   - Good as-is, verify card padding and touch targets

5. **Action buttons:**
   - Change `p-2` to `p-2.5` with `min-h-[44px] min-w-[44px]` for touch targets
   - Ensure flex-1 buttons have min-h-[44px]

**Voice Settings page:**

1. **Responsive container:**
   - Change `p-8 max-w-4xl` to `p-4 sm:p-6 lg:p-8 max-w-4xl mx-auto`

2. **Form cards:**
   - Change `p-8` to `p-4 sm:p-6 lg:p-8`
   - All input fields should already be responsive, verify padding

3. **Toggle switches and inputs:**
   - Ensure adequate touch target size (44px)
   - Form controls should stack properly on mobile

4. **Save button:**
   - Ensure min-h-[44px] and full-width on mobile: `w-full sm:w-auto`
  </action>
  <verify>
- Phone Numbers at 375px: stats in 2x2 grid, cards stack, Add button full-width
- Voice Settings at 375px: forms readable, inputs full-width, adequate spacing
- All buttons meet 44px touch target minimum
- No horizontal scroll at 320px viewport
  </verify>
  <done>
- Phone Numbers has responsive padding throughout
- Stats cards show 2-column grid on mobile
- Voice Settings forms are mobile-friendly
- All touch targets meet 44px minimum
- No content overflow on small screens
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive Voice module pages (Voice Agents, Phone Numbers, Voice Settings)</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open DevTools and set viewport to iPhone 14 (390px)
    3. Navigate to Voice Agents (/voice-agents):
       - Verify list view is visible
       - Tap an agent card - detail panel should take over screen
       - Verify back button appears at top
       - Tap back - returns to list
    4. Navigate to Phone Numbers (/phone-numbers):
       - Verify stats show in 2x2 grid
       - Verify Add button is full-width
       - Verify card actions are tappable
    5. Navigate to Voice Settings (/voice-settings):
       - Verify forms are readable
       - Verify save button is accessible
    6. Test at iPad width (768px):
       - Verify transitions work smoothly
    7. Test at desktop width (1280px):
       - Verify side-by-side layouts restored
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors
- [ ] Voice Agents has mobile master-detail toggle
- [ ] Phone Numbers has responsive grid and padding
- [ ] Voice Settings has responsive forms
- [ ] All touch targets meet 44px minimum
- [ ] No horizontal overflow at 320px viewport
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Voice module pages work well on mobile devices
- Consistent with patterns established in Phase 17-18
</success_criteria>

<output>
After completion, create `.planning/phases/18-secondary-pages/18-02-SUMMARY.md` following summary.md template.

Include frontmatter with:
- phase: 18-secondary-pages
- plan: 02
- subsystem: ui
- tags: [responsive, mobile, voice-module, touch-targets]
- patterns-established: (any new patterns)
- key-files modified
</output>

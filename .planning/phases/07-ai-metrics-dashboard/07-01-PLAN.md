---
phase: 07-ai-metrics-dashboard
plan: 01
type: auto
domain: frontend-dashboard
---

<objective>
Build the dashboard UI shell with metric cards showing real-time AI state and system intelligence.

Purpose: Establish the visual framework that displays metrics—prove the system is "thinking" in real-time with responsive, organized metric cards.
Output: Responsive dashboard layout with metric cards, ready for real-time data subscriptions and chart integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/plan-format.md
.planning/phases/07-ai-metrics-dashboard/07-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-metrics-dashboard/07-RESEARCH.md

**Tech stack available (from v1.0 MVP):**
- Next.js 16 App Router + React 19
- Supabase SSR for auth + real-time
- Tailwind CSS 4 for styling
- TypeScript for type safety

**Key decision from RESEARCH.md:**
- Use Tremor for pre-styled dashboard components (not building from scratch)
- Recharts for specific chart controls (integrated with Tremor)
- Zustand for global metrics state (established in v1.0)

**Files to reference:**
- `src/app/layout.tsx` - Root layout structure
- `src/app/page.tsx` - Main dashboard page
- `src/components/` - Existing component patterns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tremor + Recharts + Zustand dependencies</name>
  <files>package.json</files>
  <action>Add to package.json: "tremor": "^3.15", "recharts": "^2.10", "zustand": "^4.4", "numeral": "^2.0". Run npm install. Do NOT use exact versions—use caret ranges as shown. Verify installation with npm ls tremor recharts zustand.</action>
  <verify>npm ls tremor recharts zustand shows all three packages installed with correct versions</verify>
  <done>All three libraries installed and importable from node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard page layout with Tremor cards</name>
  <files>src/app/dashboard/ai-metrics/page.tsx, src/components/dashboard/AIMetricsDashboard.tsx, src/components/dashboard/MetricsCard.tsx</files>
  <action>Create responsive dashboard page showing AI system metrics. Use Tremor's Grid and Card components for layout (not custom CSS). Create at least 5 metric cards: "AI State" (idle/thinking/responding), "Conversation Active" (conversation count), "Avg Latency", "Decision Confidence", "Context Size (tokens)". Each card shows a metric name, current value, and brief description. No data wiring yet—use placeholder values (0, "idle", etc). Use Tailwind for spacing/responsive behavior. Import pattern: import { Card, Grid, Metric, Text } from 'tremor'.</action>
  <verify>npm run dev, visit http://localhost:3000/dashboard/ai-metrics, page loads without errors, all 5 cards visible on desktop (responsive on mobile)</verify>
  <done>Dashboard renders with 5 metric cards, responsive layout, no TypeScript errors, values displayed (placeholder OK)</done>
</task>

<task type="auto">
  <name>Task 3: Add real-time connection status indicator and navigation</name>
  <files>src/components/dashboard/ConnectionStatus.tsx, src/app/dashboard/layout.tsx</files>
  <action>Add a connection status badge showing "Connected", "Connecting", "Disconnected" states. Place it in dashboard header. Add simple navigation menu with links to AI Metrics page (current), and placeholder links to "HUD", "Autonomy Demo", "State Inspector" (for phases 8-10). Use Tremor's status indicators if available, otherwise simple colored badge. No real connection logic yet—just UI structure showing "Connected" by default.</action>
  <verify>npm run dev, visit /dashboard/ai-metrics, see connection status badge at top, navigation menu present</verify>
  <done>Connection status visible, navigation structure in place, ready for real-time logic in next plan</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` succeeds without errors
- [ ] Dashboard page loads at /dashboard/ai-metrics
- [ ] All 5 metric cards visible and responsive
- [ ] Connection status badge visible
- [ ] Navigation menu present
- [ ] No TypeScript errors
- [ ] No build warnings
</verification>

<success_criteria>

- All tasks completed without errors
- Tremor components properly imported and styled
- Responsive layout works on desktop (>1024px) and mobile (375px+)
- Placeholder metric values display correctly
- Connection status badge shows (no real logic needed yet)
- Navigation structure ready for future phases
- Code follows project conventions (from src/components/ patterns)

</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-metrics-dashboard/07-01-SUMMARY.md`:

# Phase 7 Plan 01: Dashboard Foundation Summary

**Built responsive AI metrics dashboard shell with Tremor components.**

## Accomplishments

- Installed Tremor, Recharts, Zustand dependencies
- Created AIMetricsDashboard component with 5 metric cards (AI State, Conversation Active, Avg Latency, Decision Confidence, Context Size)
- Implemented responsive Grid layout using Tremor (not custom CSS)
- Added connection status indicator badge
- Added dashboard navigation menu structure
- All components follow project TypeScript conventions

## Files Created/Modified

- `package.json` - Added tremor, recharts, zustand, numeral
- `src/app/dashboard/ai-metrics/page.tsx` - Dashboard page component
- `src/components/dashboard/AIMetricsDashboard.tsx` - Main dashboard layout
- `src/components/dashboard/MetricsCard.tsx` - Reusable metric card component
- `src/components/dashboard/ConnectionStatus.tsx` - Connection status badge
- `src/app/dashboard/layout.tsx` - Dashboard layout with navigation

## Decisions Made

- Use Tremor's Grid/Card components for layout (not custom CSS) per RESEARCH.md recommendation
- Placeholder values for metrics (will be wired in 07-02)
- Connection status hardcoded as "Connected" (will be real in 07-02 with Zustand + subscriptions)
- Navigation structure ready for phases 8-10 expansion

## Issues Encountered

None

## Next Step

Ready for 07-02-PLAN.md: Real-time metrics state management and Supabase subscriptions

</output>

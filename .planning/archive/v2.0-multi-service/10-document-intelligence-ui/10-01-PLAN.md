---
phase: 10-document-intelligence-ui
plan: 01
type: auto
---

<objective>
Create Upload & Process page for document intelligence service with drag-and-drop upload, template selection, and processing progress tracking.

Purpose: Enable operators to upload documents, select extraction templates, and monitor processing status in real-time.

Output: New Upload & Process page at `/upload` with drag-drop zone, template selector, file preview, and processing progress indicators.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(main)/voice-agents/page.tsx
@src/app/(main)/phone-numbers/page.tsx
@src/components/layout/Sidebar.tsx

**Reference patterns:**
- TopBar component for page headers (from Voice Agents pages)
- Card-based layouts with stats and lists (from Voice Agents)
- Grid responsive layouts (3→2→1 columns)
- Orange service colors: #F59E0B → #EF4444 (from Phase 8 design system)
- Mock data patterns with realistic sample data
- Status indicators and badges

**Design specifications:**
- Service color: Orange gradient (#F59E0B → #EF4444) - available as service-documents colors in Tailwind config
- Layout: Single page with upload zone, template selector, preview, and recent uploads
- Features: Drag-drop upload, template selection dropdown, file preview, processing progress
- Mock data: 5-6 sample documents in different processing states
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Upload page with drag-drop zone, template selection, and recent uploads</name>
  <files>src/app/(main)/upload/page.tsx</files>
  <action>
  Create new page at `src/app/(main)/upload/page.tsx` with:

  1. TopBar component with title "Upload & Process"

  2. Drag-and-drop upload zone:
     - Large centered area with upload icon
     - "Drag documents here or click to browse" text
     - Accept PDF, DOC, DOCX, JPG, PNG files
     - Show file name when hovered/selected
     - Display file size

  3. Template selector dropdown:
     - Label: "Select Extraction Template"
     - Default templates: Invoice, Receipt, Contract, Form, Custom
     - Selected template shows description

  4. Recent uploads list:
     - Cards showing recent document uploads
     - Display: File name, upload date, template used, file size
     - Status badge: Processing, Completed, Failed
     - Status colors: Amber for Processing, Green for Completed, Red for Failed
     - Action buttons: View, Delete
     - Sort by most recent first

  5. Mock data:
     ```typescript
     const recentUploads = [
       {
         id: 1,
         fileName: 'Invoice_2024_001.pdf',
         uploadDate: '2 hours ago',
         template: 'Invoice',
         fileSize: '2.4 MB',
         status: 'processing', // or 'completed', 'failed'
         progress: 65
       },
       // ... 5-6 more documents
     ]

     const templates = [
       { id: 'invoice', name: 'Invoice', description: 'Extract invoice details' },
       { id: 'receipt', name: 'Receipt', description: 'Extract receipt information' },
       { id: 'contract', name: 'Contract', description: 'Extract contract terms' },
       { id: 'form', name: 'Form', description: 'Extract form fields' },
       { id: 'custom', name: 'Custom Template', description: 'Create custom extraction' }
     ]
     ```

  6. Styling:
     - Upload zone: Orange border (dashed), hover effect with orange background
     - Buttons: Orange gradient (#F59E0B → #EF4444)
     - Status badges: Amber/Green/Red with appropriate backgrounds
     - Cards: Light background with subtle borders
     - Icons: Orange colored document icons
     - Use orange service color utilities from Tailwind config

  7. Responsive layout:
     - Upload zone stacks on mobile
     - Template selector full width on mobile
     - Recent uploads: grid 3→2→1 columns (desktop→tablet→mobile)
  </action>
  <verify>
  npm run dev && navigate to /upload
  - Page loads with TopBar showing "Upload & Process"
  - Drag-drop zone displays with upload icon and instructions
  - Template selector dropdown renders with default options
  - Recent uploads cards display with file names, dates, statuses
  - Status badges show correct colors (amber, green, red)
  - Orange colors applied throughout (buttons, accents)
  - Responsive layout works on different screen sizes
  - No console errors
  </verify>
  <done>Upload page created with drag-drop zone, template selector, and recent uploads list</done>
</task>

<task type="auto">
  <name>Task 2: Add processing progress tracking and status indicators</name>
  <files>src/app/(main)/upload/page.tsx</files>
  <action>
  Enhance the Upload page with progress tracking:

  1. Add processing progress section:
     - Show documents currently being processed
     - Display progress bars for each document
     - Show percentage complete (0-100%)
     - Estimated time remaining (if available)
     - Status text: "Processing...", "Extracting...", "Finalizing..."

  2. Progress indicators:
     - Animated progress bars with orange gradient
     - Smooth animation from 0 to 100%
     - Pulse animation for processing status badge

  3. Upload statistics summary:
     - Total documents processed this month
     - Average processing time
     - Success rate percentage
     - Failed uploads count

  4. Enhance recent uploads with:
     - Progress bar for processing documents (show 65%, 45%, etc.)
     - Animated pulse for active processing
     - Countdown timer for completed ones
     - Show extraction details link for completed documents

  5. Mock data enhancements:
     - Mix of processing, completed, and failed documents
     - Realistic progress values (10%, 45%, 78%, 95%, 100%)
     - Time-based status updates (simulate real processing)
     - Add extraction summary for completed documents

  6. Styling:
     - Progress bars: Orange gradient with shadow
     - Pulse animation: Subtle orange glow
     - Status indicators: Use consistent color coding
     - Cards maintain orange accent borders
  </action>
  <verify>
  npm run dev && navigate to /upload
  - Processing section displays with progress bars
  - Progress bars animate and show percentages
  - Status badges pulse for processing documents
  - Statistics cards show correct numbers
  - Recent uploads show progress for in-progress documents
  - Completed documents show extraction count
  - Failed documents show error icon
  - Responsive design maintained
  - No console errors
  </verify>
  <done>Processing progress tracking added with animated progress bars, status indicators, and upload statistics</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors in upload/page.tsx
- [ ] /upload page loads without errors
- [ ] Drag-drop zone displays correctly
- [ ] Template selector renders with options
- [ ] Recent uploads cards show all information
- [ ] Status badges display correct colors
- [ ] Progress bars animate smoothly
- [ ] Orange service colors applied throughout
- [ ] Responsive layout works on mobile/tablet/desktop
- [ ] All mock data displays correctly
</verification>

<success_criteria>

- Upload page created at `/upload` route
- Drag-and-drop upload zone implemented
- Template selector dropdown with sample templates
- Recent uploads list with file information
- Processing progress tracking with status indicators
- Upload statistics summary cards
- Orange service colors (#F59E0B → #EF4444) applied
- Mock data with realistic documents in different states
- Responsive design maintained
- All tasks completed with peak quality
- Ready for Phase 10-02 (Processing Queue & Data pages)

</success_criteria>

<output>
After completion, create `.planning/phases/10-document-intelligence-ui/10-01-SUMMARY.md`:

# Phase 10 Plan 1: Upload & Process Summary

**Created Upload & Process page with drag-drop upload zone, template selector, and processing progress tracking.**

## Accomplishments

- Created new page at `/upload` with TopBar component
- Implemented drag-and-drop upload zone with file selection
- Added template selector dropdown with predefined templates
- Created recent uploads list showing file info and status
- Added processing progress tracking with animated progress bars
- Implemented upload statistics summary cards
- Applied orange service colors throughout
- Integrated realistic mock data with mixed processing states

## Files Created/Modified

- `src/app/(main)/upload/page.tsx` - Complete Upload & Process page with drag-drop, templates, and progress

## Implementation Details

[Document the actual accomplishments from execution]

## Next Phase Readiness

Ready for Phase 10-02 (Processing Queue and Extracted Data pages).

All document intelligence pages will follow same patterns: orange service colors, card-based layouts, mock data with realistic scenarios.
</output>
